apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: ctfd-controller
spec:
  interval: 1m0s
  path: ./manifests/
  prune: true
  sourceRef:
    kind: GitRepository
    name: ctfd-source
  decryption:
    provider: sops
    secretRef:
      name: sops-age
